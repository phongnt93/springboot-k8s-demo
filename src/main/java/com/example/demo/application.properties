server.port=8080
spring.application.name=spring-boot-k8s-demo

# Application message
app.message=Hello from Spring Boot on Kubernetes!

# Actuator endpoints
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

